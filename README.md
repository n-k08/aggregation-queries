Created an orders table and populated it with sample data.  
Used EXTRACT(YEAR/MONTH) to group by month, SUM(amount) for total revenue,<br>
COUNT(DISTINCT order_id) for order volume,<br>
filtered by date range, <br>
then sorted chronologically.
